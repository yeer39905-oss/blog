<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Cinthia&#39;s blogs</title>
    <link>http://localhost:2054/blog/</link>
    <description>Recent content on Cinthia&#39;s blogs</description>
    <generator>Hugo</generator>
    <language>zh-cn</language>
    <lastBuildDate>Fri, 24 Jul 2026 00:00:00 +0000</lastBuildDate>
    <atom:link href="http://localhost:2054/blog/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>深度学习基础：MLP、CNN 与 RNN框架</title>
      <link>http://localhost:2054/blog/posts/mlp-cnn-rnn/</link>
      <pubDate>Fri, 24 Jul 2026 00:00:00 +0000</pubDate>
      <guid>http://localhost:2054/blog/posts/mlp-cnn-rnn/</guid>
      <description>&lt;p&gt;引言：学习过MLP（多层感知机），CNN（卷积神经网络），RNN（循环神经网络）三个框架后，可以发现，三个模型本质上都是在做：输入数据X -&amp;gt; 特征提取 -&amp;gt; 分类器 -&amp;gt;  输出
不同的地方在于特征的提取的办法不同
接下来，我会用MPL模型做MINIST灰度手写图片分类，用CNN模型做CIFAR10图片分类，用RNN做情感分类为例，来说明三个模型的各自的用处和不同点&lt;/p&gt;
&lt;h2 id=&#34;mlp&#34;&gt;MLP&lt;/h2&gt;
&lt;h3 id=&#34;架构&#34;&gt;架构：&lt;/h3&gt;
&lt;p&gt;Input X -&amp;gt; Linear1 -&amp;gt;Activation1 -&amp;gt;Linear2 -&amp;gt;Activation2 -&amp;gt; &amp;hellip;&amp;hellip; -&amp;gt;Linear  -&amp;gt; Softmax -&amp;gt;Output $\hat y$&lt;/p&gt;
&lt;h3 id=&#34;每一个epochmlp做&#34;&gt;每一个epoch，MLP做：&lt;/h3&gt;
&lt;p&gt;1.把输入的数据展成一维向量，
2.全连接层做线性变换：$Z^{[l]}=W^{[l]}A^{[l-1]}+b^{[l]}$ ，每次把上一层的输出作为下一层的输入，其中$A^{[0]}=X$
3.通过激活函数引入非线性 ：$A^{[l]}=g^{[l]}(Z^{[l]})$ ，因为线性变化的组合仍然是线性变换
4.全连接层线性变换和激活重复做，本质就是在做数学上的函数嵌套
5.完成输出层的线性计算，得到logits（原始打分）
6.通过softmax把logits转换成概率，选取概率最大的类别
7.计算损失，反向传播计算梯度
8.根据梯度更新参数&lt;/p&gt;
&lt;h3 id=&#34;minist灰度手写图片分类任务&#34;&gt;MINIST灰度手写图片分类任务&lt;/h3&gt;
&lt;p&gt;MINIST灰度手写图片，单通道，大小为$28 \times28$ ,展开成列向量就是$784 \times 1$ ,参数量比较小，适合使用MLP模型来完成
&lt;img alt=&#34;MINIST灰度手写图片&#34; loading=&#34;lazy&#34; src=&#34;http://localhost:2054/blog/deeplearning/mlpMINIST.png&#34;&gt;
把上面每一个epoch做的任务详细写：
1.使用mini-bacth的方法，每次取64张MINIST图片作训练，每张图片展开成$784 \times 1$的列向量
2.通过全连接层做特征提取，其中由矩阵乘法可知，权重矩阵W 为$n^{[l]} \times n^{[l-1]}$，偏置矩阵b是$784\times 1$，其中$n^{[l]}$表示第l层的神经元的个数，我提供的代码，一共三个全连接层，第一层128个神经元，第二层64个神经元，第三层10个神经元
3.通过ReLU函数激活，$a=\max(0,z)$ ，当z&amp;gt;0,a=z;当z&amp;lt;=0,a=0
4.把上一层的输出作为下一层的输入，做该层的线性变换，然后激活，重复上述全连接和激活
5.进行输出层的线性变换，得到$64\times 10$ 的logits矩阵，10个数字就有10个原始打分
6.通过softmax回归分类，将logits转换成概率，公式是$a_i= \frac{e^{z_i}} {\sum_{j=1}^{n}e^{z_j}}$,  其中 $e^{z_i}$ 表示这个类别的得分，$0&amp;lt;a_i&amp;lt;1$ ,$\sum_i a_i=1$ ，选择10个类别中概率最大的类别
7.计算交叉熵损失$L(y,\hat y) = -y\log(\hat y) -(1-y)\log(1-\hat y)$ ,反向传播得到参数梯度，dW和db
根据梯度，更新参数&lt;/p&gt;</description>
    </item>
  </channel>
</rss>
